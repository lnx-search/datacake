[package]
name = "hourglass"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
hourglass-data = { path = "hourglass-data" }
serde_json = "1.0.81"
rkyv = { version = "0.7.38", features = ["alloc", "validation"] }
bincode = "2.0.0-rc.1"

[workspace]
members = [
    "hourglass-data",
]
exclude = [
]