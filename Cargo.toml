[package]
name = "hourglass"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
hourglass-data = { path = "hourglass-data" }
hourglass-segment = { path = "hourglass-segment" }

[workspace]
members = [
    "hourglass-data",
    "hourglass-segment",
]
exclude = [
]