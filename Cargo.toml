[package]
name = "datacake"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
datacake-data = { path = "datacake-data" }
datacake-segment = { path = "datacake-segment" }
datacake-shard = { path = "datacake-shard" }

[workspace]
members = [
    "datacake-data",
    "datacake-segment",
    "datacake-shard"
]
exclude = [
]